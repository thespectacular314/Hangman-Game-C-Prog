// Creating the Function Protocols for the required Function used.
extern int Player_choice;
void StartHangman();
void ShowRules();
char* WordGenerator(int Player_choice, int Generate_Random_Line);
char* DescriptionGenerator(int Player_choice, int Generate_Random_Line);
void HangmanDisplay(int trials);
